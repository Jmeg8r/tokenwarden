# Pinned, tested runtime environment (Python 3.13). For a reproducible install:
#   pip install -r requirements.txt && pip install -e .
# Dev/test deps come from the "dev" extra: pip install -e ".[dev]"
starlette==1.3.1
uvicorn==0.49.0
httpx==0.28.1
anyio==4.14.0
h11==0.16.0
click==8.4.1
certifi==2026.5.20
idna==3.18
